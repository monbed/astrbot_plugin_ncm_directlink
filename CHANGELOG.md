# 更新日志

本文件记录本插件的重要变更，版本号遵循语义化版本。

## [1.3.0] - 2026-07-03

### 新增
- 选歌后新增**发送方式选择**：回复 `1` 获取直链 / `2` 发送音乐文件（`0` 取消，每步 60 秒有效）。
- 文件发送：按接口返回的实际音质格式（mp3/flac 等）流式下载，**先按「歌名 - 歌手.格式」重命名，再发送**；发送完成后自动清理本地临时文件，下载目录另有 1 小时兜底回收。
- 新增 `.gitignore`。

> 依赖说明：本插件仅使用 `httpx`，它是 AstrBot 本体 pyproject 的直接依赖（`httpx[socks]>=0.28.1`），故无需 requirements.txt。

### 修复
- 群聊中多名用户同时点歌**互相覆盖缓存**的问题：会话缓存键由 session_id 改为 `unified_msg_origin + 发送者 ID`，按用户隔离。
- 私聊场景回复序号后**默认 LLM 可能再次响应**同一条消息的问题：此前经 `context.send_message` 旁路发送不计入事件发送状态；现改为标准结果发送并在消费序号后终止事件传播。
- 歌名含空格被截断的问题：指令参数改用 `GreedyStr` 接收完整关键词（`下载音乐 Shape of You` 现在整句生效）。
- 插件停用/重载时 httpx 连接未关闭的问题：补充 `terminate()`。
- `level` 配置为空时不再向 API 发送空音质参数（回退 `jyeffect`）；`limit`/`timeout` 增加类型与取值兜底。

### 变更
- 移除已废弃的 `@register` 装饰器（框架自动识别 Star 子类，元数据以 metadata.yaml 为准）。
- 回复消息改为框架标准的 yield 结果发送，享受回复前缀 / 分段回复 / 文本转图等全局修饰；不再使用主动消息接口。
- `apiurl` 未配置时启动记录告警，指令内给出友好提示。
- 配置面板为音质等级补充下拉选项与说明。
